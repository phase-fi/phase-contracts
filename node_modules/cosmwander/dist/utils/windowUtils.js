"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getCenterColWidth = exports.clamp = void 0;
const config_1 = require("./config");
function clamp(min, max, target) {
  return Math.max(min, Math.min(max, target));
}
exports.clamp = clamp;
function getCenterColWidth(width) {
  return Math.floor(width * 0.3) > config_1.SIDEBAR_WIDTH ? width - config_1.SIDEBAR_WIDTH * 2 : Math.floor(width * 0.4);
}
exports.getCenterColWidth = getCenterColWidth;